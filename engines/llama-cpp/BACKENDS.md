# Optional backends (AMD only — not NVIDIA)

| File | When |
|------|------|
| `compose.yaml` | **Default** — Vulkan / RADV (Strix Halo); profiles: `rag`, `lab`, `bench` |
| `compose.rocm.yaml` | AMD ROCm alternate image |

Daily: sticky + coder. Optional profiles: `rag`, `lab`, `bench` (:11601/:11602).
