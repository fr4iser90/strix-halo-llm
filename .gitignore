# ── Runtime / local only ──────────────────────────────────────────────
models/
*.gguf
*.gguf.partial
.build/
result
result-*

# Secrets / machine-local config
.env
.env.*
!.env.example
!**/*.env.example

# Live model presets (machine-local). Templates: engines/llama-cpp/presets/ini/
# Regenerated: ./bench sync-models  +  ./bench capacity sync
/models.ini
/models-coder.ini
/models-lab.ini
/models-embeddings.ini
/models-extractor.ini
/models-bench.ini
/models-bench-b.ini
engines/llama-cpp/models.ini
engines/llama-cpp/models-coder.ini
engines/llama-cpp/models-lab.ini
engines/llama-cpp/models-embeddings.ini
engines/llama-cpp/models-extractor.ini
engines/llama-cpp/models-bench.ini
engines/llama-cpp/models-bench-b.ini
engines/llama-cpp/models*.ini.bak.*

# Python / tooling
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp
*~

# Docker / Nix leftovers
*.tar
*.tar.gz

# ── Bench outputs (always local) ──────────────────────────────────────
# Regenerated by ./bench index / matrix / suites. Publish snapshot → docs/ only.
output/bench/
output/logs/

# Optional A/B prompt dumps — regenerate or publish selectively
output/ab-q2-vs-36/*.json
output/ab-q2-vs-36/*.json.err

# Quality harness vendor clones (install via pip / fetch script)
.vendor/
tools/bench/quality/.vendor/
